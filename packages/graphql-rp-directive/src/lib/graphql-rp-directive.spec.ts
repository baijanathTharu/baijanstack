import { graphqlRpDirective } from './graphql-rp-directive';

describe('graphqlRpDirective', () => {
  it('should work', () => {
    expect(graphqlRpDirective()).toEqual('graphql-rp-directive');
  });
});
